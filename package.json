{
  "name": "personalwebsite",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    // The "dev" script is used to start the development server.
    "dev": "next dev",
    
    // The "build" script is used to build the production-ready version of the application.
    "build": "next build",
    
    // The "start" script is used to start the production server.
    "start": "next start"
  },
  "dependencies": {
    // Next.js is a React framework for building server-side rendered and static websites.
    "next": "10.2.3",
    
    // React is a JavaScript library for building user interfaces.
    "react": "17.0.2",
    
    // React DOM is a package for rendering React components in the browser.
    "react-dom": "17.0.2",
    
    // React Icons is a collection of popular icons for React applications.
    "react-icons": "^4.2.0",
    
    // STL Viewer is a library for rendering 3D STL files in the browser.
    "stl-viewer": "^0.12.0",
    
    // Styled Components is a CSS-in-JS library for styling React components.
    "styled-components": "^5.3.0",
    
    // Styled Normalize is a CSS reset library for normalizing styles across different browsers.
    "styled-normalize": "^8.0.7",
    
    // Three.js is a JavaScript library for creating and rendering 3D graphics in the browser.
    "three": "^0.140.0"
  }
}
